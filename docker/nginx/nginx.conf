user nginx;
worker_processes auto;

error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  # Rate limiting (IP-based)
  # Notes:
  # - limit_req_zone must be defined in the http block
  # - limit_req is applied in server/location blocks
  limit_req_status 429;
  limit_req_zone $binary_remote_addr zone=demo_start:10m rate=6r/m;
  limit_req_zone $binary_remote_addr zone=admin_exports:10m rate=10r/m;

  log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                  '$status $body_bytes_sent "$http_referer" '
                  '"$http_user_agent" "$http_x_forwarded_for"';

  access_log /var/log/nginx/access.log main;

  sendfile on;
  keepalive_timeout 65;

  # Standard Debian-style site includes
  include /etc/nginx/sites-enabled/*.conf;
}
